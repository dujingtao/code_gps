package com.codegps.app.ui.theme

import androidx.compose.ui.graphics.Color

// Background gradient stops — deep space navy, dark from top to bottom.
val SpaceBlackTop = Color(0xFF050810)
val SpaceBlackMid = Color(0xFF0A0E1A)
val SpaceBlackBottom = Color(0xFF0D1220)

// Neon accents used for interactive elements and primary data.
val NeonCyan = Color(0xFF00E5FF)
val NeonViolet = Color(0xFF7C4DFF)

// GPS fix-quality status colors (also used by ReadoutCard accents).
val StatusGood = Color(0xFF00E676)
val StatusModerate = Color(0xFFFFC400)
val StatusPoor = Color(0xFFFF5252)

// Text colors on the dark HUD background.
val TextPrimary = Color(0xFFE8F1FF)
val TextSecondary = Color(0xFF7E8CA3)

// Glass card fill/border — a thin translucent white overlay on top of the
// (optionally blurred) background, see GlassSurface.
val GlassFill = Color(0x1AFFFFFF)
val GlassBorder = Color(0x33FFFFFF)
